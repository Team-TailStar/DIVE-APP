name: dive_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

  # === 지도/위치/딥링크 ===
  flutter_naver_map: ^1.4.1+1  # 네이버맵 플러그인
  geolocator: ^11.0.0         # 현재 위치/권한
  url_launcher: ^6.3.0        # nmap:// 딥링크

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 앱에서 쓰는 이미지 에셋


  # 커스텀 폰트 쓰면 여기에 추가 (예시)
  # fonts:
  #   - family: Pretendard
  #     fonts:
  #       - asset: assets/fonts/Pretendard-Regular.otf
  #       - asset: assets/fonts/Pretendard-Bold.otf
  #         weight: 700
